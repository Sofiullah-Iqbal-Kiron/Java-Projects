// Accepted but that was not expected.
// We need to implement in own way.
// Posted in twitter.
// Ubuntu-Link:

package MonthlyChallenge2020.July;

public class PowOfxn
{
    public double myPow(double x, int n)
    {
        return Math.pow(x, n);
    }
}
